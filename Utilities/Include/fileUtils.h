#include <vector>
#include <string>

using namespace std;

// TODO: remove file or doxy!

vector<string> getDirectoryContent(const string& i_directoryName);

void createDirectory(const string& i_directoryName);
